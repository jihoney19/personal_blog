<section class="home-empty-state" aria-labelledby="empty-title">
  <div>
    <strong id="empty-title" class="mono">[ STATUS: NULL_QUEUE ]</strong>
    <p>아직 게시된 글이 없습니다.</p>
    <p class="preparing">
      <span class="mono" aria-hidden="true">[+]</span> 새로운 학습 기록을 준비하고 있습니다.
    </p>
  </div>
</section>

<style>
  .home-empty-state {
    display: grid;
    min-height: 230px;
    place-items: center;
    border: 1px dashed var(--hairline);
    color: var(--mute);
    text-align: center;
  }

  .home-empty-state strong {
    display: block;
    margin-bottom: 14px;
    color: var(--accent);
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.06em;
  }

  .home-empty-state p {
    margin: 0;
    font-size: 14px;
  }

  .home-empty-state .preparing {
    margin-top: 8px;
    color: color-mix(in srgb, var(--mute) 72%, transparent);
  }

  .preparing span {
    margin-right: 6px;
  }

  @media (max-width: 640px) {
    .home-empty-state {
      min-height: 210px;
      padding: 32px 20px;
    }
  }
</style>
